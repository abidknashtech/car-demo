apiVersion: v1
kind: Service
metadata:
  name: cartservice
  labels:
    app: cartservice
spec:
  ports:
    - name: http
      port: 9096
      targetPort: 9096
  selector:
    app: cartservice
  type: LoadBalancer